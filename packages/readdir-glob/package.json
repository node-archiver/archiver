{
  "name": "@archiver/readdir-glob",
  "version": "0.1.1",
  "description": "Recursive fs.readdir with streaming API and glob filtering.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/node-archiver/archiver.git",
    "directory": "packages/readdir-glob"
  },
  "files": [
    "dist"
  ],
  "type": "module",
  "exports": {
    ".": "./dist/index.mjs",
    "./package.json": "./package.json"
  },
  "scripts": {
    "build": "tsdown",
    "test": "node --test --import ./tests/hooks.ts --test-concurrency=1",
    "test:bun": "bun test --dots --preload ./tests/hooks.ts --max-concurrency 1",
    "prepublishOnly": "bun run build"
  },
  "dependencies": {
    "picomatch": "^4.0.4"
  },
  "devDependencies": {
    "@types/picomatch": "4.0.3",
    "tsdown": "0.23.0"
  },
  "engines": {
    "node": "^22.12.0 || >=24.0.0"
  }
}
