{
  "name": "@archiver/readdir-glob",
  "version": "0.0.9",
  "description": "Recursive fs.readdir with streaming API and glob filtering.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/node-archiver/archiver.git",
    "directory": "packages/readdir-glob"
  },
  "files": [
    "dist"
  ],
  "type": "module",
  "exports": {
    ".": "./dist/index.mjs",
    "./package.json": "./package.json"
  },
  "scripts": {
    "build": "tsdown",
    "test": "bun test --reporter dot --preload ./tests/hooks.ts --max-concurrency 1",
    "prepublishOnly": "bun run build"
  },
  "dependencies": {
    "picomatch": "^4.0.4"
  },
  "devDependencies": {
    "@types/picomatch": "^4.0.2",
    "tsdown": "catalog:"
  },
  "engines": {
    "node": ">=24"
  }
}
