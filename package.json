{
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "catalog": {
      "tsdown": "^0.21.1"
    }
  },
  "scripts": {
    "u": "bun format ; bun lint ; bun run build ; bun run test",
    "format": "oxfmt",
    "lint": "oxlint",
    "build": "bun run --filter='./packages/**' build",
    "test": "bun run --filter='./packages/**' test",
    "test-node": "bun run --filter='./packages/**' test-node"
  },
  "devDependencies": {
    "@types/bun": "^1.3.10",
    "@types/node": "^25.4.0",
    "oxfmt": "^0.36.0",
    "oxlint": "^1.51.0",
    "typescript": "^5.9.3"
  }
}
